;; This is a simple test file for the beast programming language

(module $math
  (func $add_some_values
    (if (gt %st, %st)
      (push %st, 1.0)
    (else
      (push %st, 2.0)
      (call $add_some_values))))
  (func $some_func
    (if (gte %st, %st)
      (call $something)
    (else (if (lt %st, %st)
      (push %st, 20)))))
  (export $add_some_values))

(; This is a multiline comment,
where I can write whatever I want ;)
